name: telecom_intelligence
version: "1.0.0"
config-version: 2
profile: telecom_intelligence
model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
clean-targets: ["target", "dbt_packages"]
models:
  telecom_intelligence:
    staging:
      +materialized: view
    marts:
      +materialized: table
